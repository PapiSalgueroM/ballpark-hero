# Audits

Human-readable, scannable fact-check reports for staged content (Track B).

One file per game: `<game>_audit.md`. Each lays out every candidate item field-by-field
in a table, with a source-URL column and a column for internal inconsistencies, so that
Anthony + his chat assistant can fact-verify quickly against live sources.
