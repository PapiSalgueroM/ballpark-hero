# Session log — 2026-07-10 overnight (Cowork autopilot, new computer)

Owner gave full autopilot ~1:10am ET. Eight pushes shipped; everything through
push 5 is PUBLISHED on douknowball.com, pushes 6-8 need one more publish
(deploy_project) after Lovable builds them.

## Machine setup (this computer is now the primary dev box)
- Local repo synced to origin/main after being 122 commits behind; stale May
  working tree backed up to Cowork outputs (local-changes.patch) then reset.
- Native build-verification env in the sandbox at /tmp/bp: `npm install`,
  `tsc -p tsconfig.app.json --noEmit` (baseline: 37 PRE-EXISTING errors in 11
  old files — new code must add zero), then `vite build` (plugin-react swapped
  for plugin-react-swc there because @swc/core SIGBUSes in the sandbox; repo
  config untouched). Run tsc/vite FOREGROUND with `timeout 43` — background
  runs die silently.
- ⚠️ MOUNT GREMLIN: host-side file writes (Cowork Write/Edit tools) can race
  the sandbox mount — files got TRUNCATED mid-write three separate times
  (App.tsx, gameRegistry.ts, sitemap.xml, nba-chain repo copy, SignThePlayer
  rewrite). RULE: write source via bash/python heredoc, and verify
  `tail -c`/byte-count before every git add. Verify committed blobs with
  `git show HEAD:file | tail -c 30`.
- Pushing: sandbox has no GitHub creds. PUSH_LIVE.bat in repo root (untracked)
  pushes with the Windows credential store; Claude runs it via the Run dialog
  (computer-use, already granted this session) and CONFIRMS by `git fetch` +
  comparing SHAs. Publishing: Lovable MCP deploy_project
  (c29d224f-a662-4a15-b809-d86fa3b3f0ad) after get_project shows the commit
  built; verify live via douknowball.com/sitemap.xml.

## Shipped tonight (pushes 1-8)
1. nba-chain-validate: GEMINI shim (last of 16 validators) + repo copy sync.
2. NEW GAME: Dart Draft (/dart-draft) — timed two-phase dart throws, 12-wedge
   board (leagues/nations/LEGENDS), ring tiers pull player quality, 11 throws
   fill a formation, local best-of-3 sim vs The Machine. Play-tested on the
   id-preview URL: throw → wedge → player → roster all work.
3. NEW GAME: Blind Rank (/blind-rank) — KOT4Q format, 5 reveals placed blind,
   ET-seeded daily + unlimited, staggered truth reveal. Play-tested live:
   completion pipeline fired (header points/rank updated as guest).
4. Fix batch: F1 Driver clues reordered vague→specific (all 20), Stat
   Detective 6-step hint ladder (career span/franchise count/initials — no
   more restating the case file), Draft Guesser final-player reveal no longer
   skipped.
5. Transfer Path: TEMPORAL teammate validation (same club AND same season —
   the Rooney→Ronaldo→Mbappé bug). DB audit vs the real teammate graph:
   51 trivial direct pairs + 17 broken puzzles DELETED, 439 min_steps
   corrected, backup in transfer_path_puzzles_backup_20260710. WC Bracket:
   real playoff winners baked in (Czechia, Bosnia, Turkey, Sweden, Iraq,
   DR Congo — all web-verified), playoff-picks panel retired.
6. NBA Conquest: map surgery — TX gets a 3rd territory (SAS out of New
   Mexico), NorCal split (SAC out of Nevada), NM→PHX; NFL map untouched via
   NFL_STATES/NBA_STATES split in usStatesPaths. Vucevic resolved to July
   2026 truth (ORL homecoming; Simons to CHI — the real trade; Queta anchors
   BOS).
7. Offline judge (localLineupEval.ts): Build-Your-XI + NBA Starting 5 always
   produce a verdict when the AI referee is down/out of quota (market-value /
   bref-peak local evaluators).
8. Sign the Player REBUILT as the box2box Auction House: 3 bidders × £1B,
   themed 33-player pools (Current/Legends/WC2026), good-first-then-superstar
   lot order, leftover assignment fees, AI bidding personalities (The Sheikh
   overpays, Moneyball Mike hunts value), live ticker, double round-robin
   showdown + Golden Boot. Old value-guessing game retired.

Also (no push needed):
- Squad Deal rework shipped in push 5's batch: 42 more legends (10+ per
  position), legend rating curve 85-99, topics (WC2026/PL/LaLiga/SerieA/
  CONMEBOL), extras are now real-name mini Deal-or-No-Deal boards
  (Pep/Fergie/Klopp, Camp Nou/Bombonera, the Kop/La Doce), explicit
  STAY/SWAP at final 2 (players AND extras), full names on eliminated cases,
  era-aware banker/sim, and global points credit (was missing entirely).
- daily_polls: Morocco removed from SF polls after France 2-0 (real result);
  scheduled Cowork task "wc-polls-daily-fix" runs 9:07am ET daily through
  Jul 20 (needs the Cowork app open) — web-checks results, fixes rows by SQL.
- GEMINI_API_KEY confirmed present and working; free-tier quota exhausts
  daily (429 ~evening, resets midnight PT). All 16 validators shimmed;
  games fail open. Offline judge covers the two evaluators.

## Queued next (priority order)
1. My Player / Soccer Career era overhaul — the BitLife-beater brief (era-
   locked opponents that advance with time, position-based starting stats,
   era-correct Ballon d'Or top 30, capped starting ratings, WAY more life
   events: relationships/kids/scandals/interviews/streakers/rivals/child
   support, injuries that fire). BIGGEST remaining item.
2. NFL Conquest overhaul list (abbr labels centered, rugged borders, FA pool
   rules, choosable power-up targets, adjacency-only matchups, eliminated
   teams out of power rankings, battle-log accuracy, 2026 ratings).
3. 17-0 / 82-0: decade mode, defense, playoff/Super Bowl runs, MVP,
   postgame analysis, kinder records.
4. Full NBA Conquest roster verification vs 2026 reality (only anchors were
   web-verified tonight); same for NFL rosters.
5. Blind Rank multi-sport modes (NBA PPG/rings via bref table).
6. Start/Bench/Cut + Guess-the-Stadium + Minefield builds
   (docs/YOUTUBER_FORMATS.md has the specs).
7. Data expansion: bulk import beyond top-600 market values so "every player
   can be guessed"; validation-cache table (player,row,col)->verdict to slash
   Gemini quota burn on the grids.
8. Career Ladder hint-economy rebalance (Xavi Simons 550-pts complaint),
   Guess-the-Club niche expansion, Millionaire + Connections content batches.

## Play-test list for the morning
/dart-draft (full 11 throws + sim), /blind-rank daily, /sign-the-player full
auction (watch AI budget behavior), /squad-deal legends era ratings + extras
boards + stay/swap, /transfer-path daily (temporal chains), /world-cup-bracket
(groups show Czechia/Bosnia/Turkey/Sweden/Iraq/DR Congo, no playoff panel),
/conquest-nba (SAS in Texas, SAC in California), /f1-driver hint order,
/stat-detective hint ladder, /football-draft 5th reveal, /build-your-xi with
quota out (offline judge verdict).
