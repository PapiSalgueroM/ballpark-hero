# Round 614: what the two attackers found in the contract

Written 2026-09-16. The contract beside this file was drafted from four read only
investigations and then attacked by two independent agents. Their findings are recorded
here verbatim rather than folded into the contract, because several of them contradict
the contract and one contradicts the board. Nobody should build from the contract until
these are resolved. Nothing here has been verified by the lead yet.

## attack:1


## attack:2


## attack:1


## attack:2

### The offline suite cannot put the real hook on any date but today, and on 92 of 120 dates the unknown bot has nothing to submit
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\hooks\useFootballGrid.ts

Section 4d specifies driving 'a fixed list of dates (at least 30)' through the real hook because 'dailyBoardFor(todayStr) is already exported'. The hook takes no date: useFootballGrid.ts:89-90 does `const todayStr = useMemo(() => getTodayET(), [])` then `dailyBoardFor(todayStr)`, and useDailyPuzzle.ts:275 computes its own `getTodayET()` for the storage key. There is no module to mock the way collegeGridOffline.test.tsx mocks `@/data/collegeGridPuzzles` (the NFL has no pool). The suite as written replays today's board 30 times. It must mock `@/lib/dateUtils` or fake the clock, and the contract must say so. Worse, normal difficulty deals exactly one achievement and only three of the twelve (first, undrafted, late) can ever return unknown once positions are complete. I built the boards for 120 consecutive dates from the real PRNG: only 28 deal a draft criterion. Of the four dates the contract names, three can never produce an unknown cell, so the unknown bot passes vacuously on them. The suite has to pick dates by what they deal, print how many unknown-capable dates it found, and refuse to run at zero.

Evidence: Board achievement per named date, rebuilt with mulberry32 and buildFranchisePuzzle: 2026-09-02 pos:DL, 2026-09-03 pos:DB, 2027-01-01 pos:WR, 2026-10-15 late. Over 120 dates from 2026-09-16: {first:13, undrafted:4, late:11, sb:10, positions:82}, so 28 of 120 are unknown-capable. On all 28 a player exists whose verdict is unknown on a dealt cell (thinnest best cell 219 players).

### Won a Super Bowl is declared complete with no unknown, while the contract's own residue proves it is not
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\lib\collegeGrid.ts

Section 2c's table says Won a Super Bowl is 'never unknown, because the record is complete after 2d', and the done-measures say '0 careers are unknown on Won a Super Bowl'. Section 2d then records that the name-only join finds 104 careers and the strict join credits 89, so 15 are namesakes left uncredited, plus 'a winner whose post 1969 rows carry a different spelling or no birth date'. Those careers will ship a definite NO built on a fact the round knows is incomplete, which is precisely the defect the round exists to remove. Round 611 solved the identical problem rather than recording it: collegeGrid.ts:214-219 sets identityOpen and lines 240, 246 and 250 turn a no into unknown for split identities. The NFL round drops that pattern instead of reusing it. The generator already has to compute the loose-join candidate list to print it, so marking those careers sbOpen and judging unknown costs nothing. Note also that an undercount is invisible to a `sbWins > 0` judge: Herb Adderley reads 1 for a three-title career and still judges yes, so the harness pin on Adderley is the only thing in the fence that can see an undercount at all, and it only sees his.

Evidence: Contract 2d: 'the name only join finds 104, so 15 candidates are namesakes and are not credited'; contract 2c: 'never, because the record is complete after 2d'. The key carries 1,584 careers flagged dup, and 126 of the 1,712 careers whose first key season is 1970 to 1972 share a folded name with another career.

### One draft slot is held by two careers 47 times, and the fence as specified is structurally blind to it
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\genNflGridData.mjs

genNflGridData.mjs:240-249 resolves a career's draft by searching `picksByName` on the normalised name and taking the row whose pick equals the roster draft_number, so two careers sharing a name can take the same row. Measured on the live key: 47 (draft_year, draft_pick) slots are held by more than one career, 96 careers sit on one, and 6 of those slots are inside their year's first round, so 12 careers will judge First Round Pick YES off a slot that belongs to one man. At least half of those twelve are wrong by construction. Section 7 as specified ('firstRound equals inFirstRound(year, pick, ends)') compares the derivation with the stored pick and will confirm every one of them, because it never asks whether the pick belongs to that person. Round 611 wrote exactly this check for the College Grid (simCollegeGridKey.mjs section 9, 'One draft slot, one person', with the noslotjoin control); Round 614 adds no equivalent, so the round ships a 'derived, never copied' claim over a key that still assigns one first-round pick to two people.

Evidence: 47 shared slots, 96 careers, 6 inside the first round (2018 pick 7, 2018 pick 30, 1992 pick 19, 1987 pick 28, 1987 pick 18, 1985 pick 11), 12 careers judging first-round yes. Concrete case: nflGridPlayers.json gives both 00-0015325 (Tony Smith, KC, 1992-1992) and 00-0015326 (Tony Smith, ATL/CAR, 1992-1995) draft {year:1992, round:1, pick:19}; collegeGridPlayers.json separates them correctly, giving 00-0015325 draft row [1992,159] and 00-0015326 [1992,19].

### Section 8's two-key metric is defined so that it cannot see the only real disagreement, and its baselines do not reproduce
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simCollegeGridKey.mjs

Section 8 says today's disagreement is '305 over the 22,008 shared ids, 273 one way and 32 the other' and sets the post-round baseline at 'zero definite opposites', with 'a null on either side is legitimate'. Measured on the two committed keys: 273 opposites today, all NFL-true/College-false, and 0 the other way; the 32 does not reproduce. After deriving, exactly one field-level opposite survives, and it is the Tony Smith slot collision above. But the College Grid key marks all three Tony Smith entries identityOpen, so judgeLabel returns unknown rather than false, and under section 8's own definition ('never a definite opposite', null legitimate) that opposite disappears and the section reads zero. So the check is green on day one, achieved by the other key hedging, while a man drafted 159th judges First Round Pick YES. This is the 'assert non-significance' trap in a new shape: the metric gets easier the more the other key says unknown. Either compare at field level and set the baseline at the measured 1 with the case named, or replace the section with the one-slot-one-person check from finding 3. As written it is a gate that proves nothing and a number nobody measured.

Evidence: Computed over the 22,008 shared ids: today 273 opposites (273 NFL true/CG false, 0 the other way); after deriving 1 (Tony Smith (Notre Dame), 1992 pick 19, NFL true, CG first_round false). All three 'tony smith' College entries have identityOpen true, so judgeLabel yields unknown, not no.

### The nounknown control cannot fire against the check it is assigned to
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simNflGrid.mjs

Section 4b's new section 7 is specified as: count yes/no/unknown per criterion, print them, and 'fail if any unknown is not one of the three named causes', with the explicit instruction 'Do not assert that the unknown share is small; assert that every one of them is explained.' Its control is `nounknown` (make the judge return no instead of unknown), expected to make 'the explained count collapse, and the section go red'. Turning every unknown into a no leaves zero unknowns, therefore zero unexplained unknowns, therefore the section stays green. A collapsing explained count is not a failure condition unless section 7 also asserts a floor on the unknown counts, which the contract forbids in the next sentence. The control as written proves the check is dead rather than alive. The control that actually exercises this check is the inverse: turn some definite no into an unknown with a cause not on the list, and require section 7 to name it.

Evidence: Contract 4b: 'fail if any unknown is not one of the three named causes ... Do not assert that the unknown share is small' followed by 'Control: nounknown (make the judge return no instead of unknown; the explained count must collapse, and the section must go red).'

### simNflGridData section 2 goes red on the Super Bowl backfill and the contract does not touch it
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simNflGridData.mjs

Section 2 rebuilds a sample through the generator with `gen.buildKey({ rosters: rows, oldRosters, stats: [], picks: [], codes, superBowls })` (line 135) and then compares `r.sbWins !== p.sbWins` (line 150). The pre-floor title pass takes a new `titleRosters` input, so this call rebuilds every sampled career without its 1966 to 1969 titles and any sampled career among the 89 fails. The sample is every 500th player (44 careers), so the failure is intermittent and will read as a data break rather than a harness that was not updated. Contract 4a lists changes to sections 1, 3 and 6 only. Section 2 must pass the four winner rosters. Separately, once buildKey runs cleanDraftPicks and roundStarts internally (3b item 1), `picks: []` gives every year no boundary, so the rebuilt firstRound, round and roundMin are all null; section 2 does not compare them today, so this is latent, but section 7 must do its own fresh pull rather than reuse this rebuild.

Evidence: simNflGridData.mjs:135 `const rebuilt = gen.buildKey({ rosters: rows, oldRosters, stats: [], picks: [], codes, superBowls });` and :150 `if (r.sbWins !== p.sbWins) fail(...)`.

### roundOf returns exact:true inside a missing round, and the contract's block measurement does not match the table
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\lib\draftRounds.mjs

Section 2a defines `{ round: r, exact: true }` when `start(r) <= pick < start(next known round)`. When a round has no row at all, the next known round is not r+1 and every pick in the gap gets an exact round that is really a lower bound. Measured: 623 round blocks, 0 overlapping, and 2 non-contiguous, not the '2 overlapping, 4 not contiguous, 3 of those since 1970' the contract records. The two are 1946 (round 6 starts at pick 41, next known round is 30 at pick 281) and 1971 (round 10 starts at 235, next known round 16 at 391). 40 careers in the key sit inside those two spans and would be stored with a wrong exact round in the re-meant draft_round column. Both criteria the page ships survive by luck (both spans are past the first-round boundary and at or past round 6, so first NO and late YES are both right), but section 1's new okDraft is specified as 'an integer that roundOf reproduces exactly', which compares the derivation with itself and can never see this. roundOf should return exact:false when the next known round is not r+1, and section 7's ratchet should be set from the measured 2, not 4.

Evidence: Over cleaned (year,pick)-deduplicated rows: 623 blocks at round>=2; 0 where the next round's start is at or below this round's start; 2 where the next known round is not round+1, namely 1946 round 6 (41) to round 30 (281) and 1971 round 10 (235) to round 16 (391); 40 nfl_grid_players careers have a pick inside those spans.

### The position unknown branch is dead, and the judge table and the new section 7 disagree about it
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\lib\nflGrid.ts

Section 2c lists 'unknown when pos is empty' for the eight position criteria and then says to measure first and not write the branch if nothing can reach it. The measurement is zero: no career in the 22,008 has an empty pos, and the column is `pos text[] not null default '{}'` with the key never producing an empty list. So the branch must not be written, the judge table should say never for positions, and section 4b's new section 7 should say so too, because its three named causes (no boundary year, no draft record, lower bound under 6) do not include an empty position list, so a branch that did fire would be reported as an unexplained unknown and fail the round. This is also the root cause of the first finding: with positions always complete, only 3 of the 12 achievements can ever produce an unknown, which is why most dates have no unknown-capable cell.

Evidence: Over scripts/data/nflGridPlayers.json: 0 of 22,008 players have an empty pos array. Verdict tally under the contract's judge: sb yes 2383 / no 19625 / unknown 0; first 1646 / 15314 / 5048; undrafted 4534 / 12718 / 4756; late 4272 / 12688 / 5048; all eight position groups unknown 0.

### Two guide lines the round makes false are not on the copy list
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\data\gameContent\football.ts

Section 3f names only the FAQ line 'so a cell never depends on a guess about a guess' and the dialog's 'Wrong answers turn red. Retry costs a guess'. Two more lines in the same guide entry become false the moment an unknown stops charging: howToPlay says 'Budget carefully: you get 15 guesses for all 9 cells, and every submission counts, so you can only afford 6 misses', and the FAQ 'Do correct answers use up guesses too?' answers 'Yes. The 15 guess budget counts every submission, so a perfect game spends 9 and leaves room for exactly 6 mistakes.' Both state the exact rule the round changes. Since the copy changes anyway, genSearchKeywords must be rerun (the contract does say this), but the list of lines to fix has to include these two or the guide ships a rule the page no longer follows.

Evidence: src/data/gameContent/football.ts, '/football-grid' entry: howToPlay item 4 and the second FAQ answer both assert that every submission counts.

### The copy-claims parser cannot be copied from the College Grid: the NFL examples run in both orders
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\pages\FootballGrid.tsx

Section 4c's new section 7 says the six page claims 'are in the Row + Column = Name, Name shape simCollegeGridPage already parses'. That parser (simCollegeGridPage.mjs:273) matches `^(.+?) \+ (.+?) = (.+)$` and then requires the first half to be a school and the second a criterion. Two of the six NFL examples put the criterion first ('First Round Pick + Played for Colts = Peyton Manning' and 'Round 6 or Later Pick + Played for Patriots = Tom Brady'), and all six spell the franchise as 'Played for Cowboys' rather than the pool label 'Cowboys'. Copied verbatim the parser rejects two of six and cannot resolve any franchise. It needs to accept either order and to build the 'Played for ' + label set the way simNflGridPage.mjs:132 already does. All six claims do survive the new rule on the data (Brady 2000 pick 199 is at or past that year's round 6 start of 167; Manning 1998 pick 1 is inside the boundary of 30), so this is a harness-shape problem, not a copy problem.

Evidence: src/pages/FootballGrid.tsx:152-159 examples array; simCollegeGridPage.mjs:273 `line.match(/^(.+?) \+ (.+?) = (.+)$/)` with row required to be `kind === 'college'`; simNflGridPage.mjs:132 builds `Played for ${c.label}`.

### The name_norm plan silently rewrites 1,641 rows and re-means a join in Round 613's purge SQL
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\docs\design\round-613-grid-cache-purge-candidates.sql

Section 3b item 6 says to compute nameNorm with the generator's own normalizeName over the display name. That cannot reproduce what is stored: 1,641 of the 22,008 rows end in a space (for example `terry miller 1970 1974 ` for display name `Terry Miller (1970-1974)`), and normalizeName (genNflGridData.mjs:112) ends in .trim(). So the plan is a rewrite, not a reproduction, and section 4a then adds name_norm to the section 6 comparison, which will pass only because the reload wrote the new values. Nothing in src or scripts reads the column, but docs/design/round-613-grid-cache-purge-candidates.sql:99 joins `nfl_grid_players g on g.name_norm = trim(regexp_replace(lower(unaccent(...)), '[^a-z0-9]+', ' ', 'g'))`, which today cannot match any of those 1,641 rows and after the change will. That query's results move without anyone touching it. Decide the rewrite deliberately, say so on the migration, and re-run or re-read the Round 613 purge candidates.

Evidence: select count(*) from nfl_grid_players where name_norm like '% ' returns 1641; sample value 'terry miller 1970 1974 '. genNflGridData.mjs:112 normalizeName ends with .trim().

### Section 3's famous careers compare the whole draft object, so all eleven drafted pins go red on the new fields
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simNflGridData.mjs

simNflGridData section 3 compares with `JSON.stringify(got) !== JSON.stringify(want)` (line 200) over each key of `expect`, and eleven of the thirteen cases carry `draft: { year, round, pick }`. Adding firstRound, round and roundMin to the draft object fails every one of them until each expectation is rewritten. Section 4a does say the thirteen 'gain the new fields', but the reason is worth stating in the contract: the comparison is whole-object, not field-by-field, so a builder who updates the generator and not the harness gets eleven failures that look like a data break. Kurt Warner (draft 'undrafted') and Art Monk (no draft expectation) are unaffected.

Evidence: simNflGridData.mjs:198-201, `for (const [k, want] of Object.entries(c.expect)) { const got = p[k]; if (JSON.stringify(got) !== JSON.stringify(want)) fail(...) }`.

### The offline suite is hung off the wrong harness, and npm test will run it
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simNflGrid.mjs

Section 4d puts the new vitest suite under simNflGrid. That harness reads only the committed key file and never touches the database, which is why it is fast and why runAllSims can run it three-up; the College Grid equivalent is driven by simCollegeGridKey, the database harness. Adding a 900-second vitest run to simNflGrid also means each of its controls pays for the whole suite before exiting: simNflGrid.mjs:196 exits 0 as soon as the control's section has fired, after the suite has already run. simNflGridData is the better host, since it already pulls the tables and is already slow. Separately, vitest.config.ts includes `src/**/*.{test,spec}.{ts,tsx}` and package.json's `test` is `vitest run`, so src/test/nflGridOffline.test.tsx runs on every plain npm test alongside the existing 900-second College Grid suite. Also worth correcting: section 2c and 4e both name 'the archive generator' as a consumer that must keep working, and there is none. Neither genGridArchive.mjs nor simGridArchive.mjs mentions football-grid or nflGrid, which section 7 half-acknowledges when it says an NFL archive would have to wait for this round.

Evidence: simNflGrid.mjs never imports the REST helper and reads only scripts/data/nflGridPlayers.json; simNflGrid.mjs:196 `if (failures[own] > 0) { ...; process.exit(0); }`. vitest.config.ts `include: ["src/**/*.{test,spec}.{ts,tsx}"]`; package.json `"test": "vitest run"`. Grep of scripts/ for football-grid or nflGrid returns 16 files, none of them genGridArchive.mjs or simGridArchive.mjs.

### Checked and clean: no board becomes unsolvable, and the contract's headline counts all reproduce
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\simNflGrid.mjs

Every yes-count that falls stays far above both bars in simNflGrid. First Round Pick drops from 2,193 to 1,646 and its thinnest franchise is HOU 56; Round 6 or Later rises from 3,917 to 4,272 with thinnest BAL 106; Undrafted is unchanged at 4,534 with thinnest PIT 167; Won a Super Bowl only rises from 2,383 and its thinnest is CIN 70; the thinnest position cell is Quarterback at 38. Section 3's bar of 30 and section 6's bar of 20 both hold with room. No existing board loses an answer it had, because judgeNflCell returns yes on exactly the set playerMatchesCell returns true on today minus the 275 false first rounders and 292 boundaryless ones, plus the 20 denied. Board ids are date seeds and the engine sequence is untouched, so no rarity row or saved board moves. The contract's measured claims reproduce exactly: 275 filed round 1 past their boundary, 292 in a year with no boundary, 20 denied first rounders (all 20 carrying a NULL round, as claimed, and the named list matches person for person), 1,646 derived first rounders, 3,917 to 4,272 late with 355 denied and 0 falsely accused, 4,756 careers with no draft record, 4,534 undrafted, 26,939 clean rows from 28,015 raw with 75 forfeits, 60 super_bowls rows from 1967, and football-grid completions 19 all time with 2 in 30 days.

Evidence: Derived per-franchise yes floors: first HOU 56, CAR 75, BAL 86, JAX 88; late BAL 106, HOU 123, CAR 134, JAX 164; undrafted PIT 167, CIN 175, BAL 213, MIN 213; sb CIN 70, HOU 74, JAX 79, MIN 82; positions QB 38, TE 41, RB 73, WR 83, DL 106, LB 107, OL 118, DB 147. The contract's floor table omits CIN 175 on Undrafted but is otherwise exact.

- Open question: Section 8 never says whether 'definite opposite' is measured on the stored first_round fields or on the two judges' verdicts. At field level the post-round baseline is 1 (Tony Smith); read through judgeLabel, identityOpen turns the College side into unknown and the baseline is 0, which makes the check green while the underlying defect stands. The contract must pick one, and the field-level reading is the only one that can see anything.
- Open question: I did not reproduce the 89 careers who won Super Bowls I to IV, because that needs the four nflverse roster CSVs downloaded and the task barred running the generator. The 89/104 split, the per-game counts (24/29/28/37) and the six careers with first season 1971 are taken from the contract on trust. Whoever builds this should re-measure them and print the 15 loose-only namesakes by name before writing any pin.
- Open question: What verdict should a career on a shared draft slot get? 47 slots are held by two careers and 12 of those careers will judge First Round Pick yes off a slot that belongs to one man. The options are the College Grid's identityOpen treatment (unknown for both), dropping the draft from the career the roster cannot confirm, or a one-slot-one-person section that fails the round until the generator picks a winner. The contract chooses none of them because it does not know the case exists.
- Open question: Round 613's purge-candidate SQL joins nfl_grid_players on name_norm. Is that query still live or already spent? If it is going to be re-run, it needs re-reading after this round rewrites 1,641 of those values, and if it is spent the file should say so.

## attack:1


## attack:1

### WRONG: the Super Bowl split 24/29/28/37 describes 95 careers, not the 89
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\scripts\genNflGridData.mjs

Section 1c and 4a section 9 present "89 distinct careers ... (SB I 24, II 29, III 28, IV 37)" as a breakdown of the 89. It is not. I reproduced the contract's own strict join (name plus birth date, through the bridge, OLD_ROSTER_STATUSES, currentCodeFor) and got 24 + 29 + 28 + 37 = 118 credits spread over 95 distinct careers. Six of those 95 already hold a title and so add a title without adding a career: Herb Adderley (1), Lee Roy Caffey (1), Bill Curry (1), Forrest Gregg (1), Marv Fleming (2), Gloster Richardson (1). 95 minus 6 = 89 newly credited, which is where the 89 comes from. A builder writing the section 9 pin as sum(perGame) === 89 will go red on correct code. The three honest pins are: 95 careers credited, 118 credits, 89 newly credited. Title sum goes 3,119 to 3,237; careers with a title goes 2,383 to 2,472 (that one the contract has right).

Evidence: scratchpad/r614/sbBackfill.mjs against the four downloaded roster files and the committed key: "SB I season 1966 GB: counted 41, STRICT in key 24", II 29, III 28, IV 37; "distinct careers matched strictly: 95"; "of those with sbWins 0 today: 89"; "matched careers that ALREADY have a title: 6 Herb Adderley(1), Lee Roy Caffey(1), Bill Curry(1), Forrest Gregg(1), Marv Fleming(2), Gloster Richardson(1)". Live: select count(*) filter (where sb_wins>0) = 2383, sum(sb_wins) = 3119.

### The 104 and 15 loose-join figures reproduce only under a restriction the contract never states

Section 2d says "the name only join finds 104, so 15 candidates are namesakes". A name only join over the whole key gives 112 distinct careers and 17 extras. Restricted to careers that carry sbWins 0 today it gives exactly 104 and 15. So the figures are right but they silently count only zero-title careers, and the loosetitles control in 4a section 9 ("the credited count must rise from 89 to 104") is only coherent under that same restriction. Say so in the control, or it will be written against 112 and go green for the wrong reason. The namesake description is correct: the extras include three Aaron Browns and three Bob Browns once the strict match is counted in.

Evidence: Node over scripts/data/nflGridPlayers.json plus the 1966 to 1969 rosters: "strict all 95 strict sbWins0 89 / loose all 112 loose sbWins0 104 / loose-only extras sbWins0 15". Extras named: Tom Brown (1987-1987), Tom Brown (1987-1989), Bob Brown (1970-1973, LA), Bob Brown (1970-1973, ARI), Willie Davis, Steve Wright, Jim Flanigan, Travis Williams, Steve Thompson, Jim Turner, Randy Rasmussen, Johnny Robinson, Aaron Brown (2009-2010), Aaron Brown (1978-1987), Jerry Mays, Bobby Bell, Mike Garrett.

### DESIGN DEFECT: re-meaning draft_round nulls it for 1,825 careers, 1,546 of which are correct today

Section 2a makes roundOf return {round: rMax, exact: false} for any pick at or past the year's largest known round start, and 2b then writes round: null whenever the round is only a lower bound. Every pick in the LAST round of its draft therefore loses its exact round. Measured on the live table: 1,825 key careers sit at or past their draft year's last known round start, and 1,546 of them already carry the correct round in today's column. draft_round is non-null for 12,514 careers today; after this round it would be non-null for roughly 10,968. Nothing in "what done measures" would catch that loss. It does not break the judge (1,533 of the 1,825 are in years whose last round is 6 or later, so Round 6 or Later still answers yes through roundMin) but it guts the column for any later consumer and for the archive the follow-ups contemplate. The block IS bounded: the year's own max pick closes it. Treating start(rMax) <= pick <= maxPick(year) as exact recovers all 1,825 without weakening anything, and the truncation risk the lower bound guards against is already covered by the 623-block ratchet in section 7.

Evidence: SQL over cleaned picks: last-round block per year, joined to nfl_grid_players gives careers_in_last_known_round 1825, of_those_col_already_correct 1546, last_round_ge6 1533, last_round_lt6 292, col_round_not_null 12514. Tom Brady survives (2000 round 7 starts at 207, his pick 199 sits inside the bounded round 6 block 167 to 206), so simNflGridData section 3's pin does not catch this.

### WRONG: 503 does reproduce, and the "515 above 28" figure does not exist

Section 0 correction 1 says "503 does not reproduce under any method tried; the nearest figures are 509 (round 1 with a pick above 32) and 515 (above 28)". 503 reproduces exactly as draft_round = 1 and draft_pick > 36, so the board's number almost certainly came from a flat pick-36 cutoff rather than being unreproducible. And "above 28" is 626, not 515. I scanned the whole neighbourhood and 515 appears at no threshold. Correct the paragraph or a reviewer will re-derive it and lose an hour.

Evidence: select on nfl_grid_players where draft_round=1: >24 831, >26 724, >=28 673, >28 626, >=32 532, >32 509, >36 503, >40 497.

### The 305 two-key figure is right, but 12 of the 32 are a hole the round leaves open
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\lib\nflGrid.ts

305 reproduces only under the shipped judge's model, where a null or absent NFL round reads as "no" (src/lib/nflGrid.ts line 155 is literally player.draftRound === 1). Under a model that judges only careers carrying a draft object it is 293. Worth stating in section 8 so the harness picks the same model. More important is the composition: the 32 is 20 plus 12, not one class. The 20 are the named first rounders with a NULL round, repaired by the derived rule. The other 12 are careers where the NFL key has NO draft record at all while the College Grid key holds a first-round pick off the same nfl_draft_picks table. After this round those 12 become UNKNOWN on the NFL side, so section 8's "zero definite opposites" passes while twelve men who plainly went in the first round still go unanswered on the NFL grid. They include Jim Kelly (1983 pick 14), Bo Jackson (1986 pick 1), Tom Cousineau (1979 pick 1), Billy Ray Smith Jr (1983 pick 5), Keith Millard (1984 pick 13), Gary Anderson (1983 pick 20), Mike Schad (1986 pick 23), Irv Smith, James Williams, Kevin Smith, Reggie Brown, Billy Cannon.

Evidence: Node over both committed keys: MODEL A (shipped judge) nflYes/colNo 273, nflNo/colYes 32, total 305, colNull 9195 (the 9,195 matches the contract). MODEL B (only careers with a draft object) 273 and 20, total 293. Split of the 32: "colYes and nfl no-draft-record 12 | colYes and nfl undrafted 0", the other 20 being the NULL-round first rounders.

### 520 of the 4,756 "no draft record" careers are recoverable and the contract treats all of them as irreducible

Section 2e and the done measures write off 4,756 careers as permanently unknown on all three draft criteria, and section 7 records only the 1968 and 1969 drafts as needing a second source. But Round 611's identity path already resolved a draft pick for 520 of those 4,756 off the same table: 12 with first_round true, 425 with first_round false, 83 with no boundary. That is a tenth of the unknown population, it includes Jim Kelly and Bo Jackson, and it costs nothing but reading a file the generator already opens. Either fold it in or record it as a named follow-up with the number attached, because "no draft record" currently reads as "the repo cannot know" when the repo does know.

Evidence: Node over scripts/data/nflGridPlayers.json and scripts/data/collegeGridPlayers.json: "NFL careers with NO draft record: 4756 / of those, College Grid holds a draft pick: 520 (first_round true 12 false 425)".

### "Won a Super Bowl has no unknown at all" overstates what the backfill delivers

Section 2c claims the criterion becomes complete and section 2d says the judge never returns unknown. The judge indeed never returns unknown, but the join that makes the fact "complete" is a name plus birth date join, and section 2d's own residue paragraph concedes that a winner whose post-1969 rows are spelled differently is not credited. So a residual wrong NO survives, and the round's own rule in 2c ("only a complete fact says no") is not strictly met for this criterion. The exposure is spelling only, not missing birth dates: every counted row in all four files carries one. Word it as "complete up to name spelling" and keep the printed residue.

Evidence: sbBackfill.mjs per season: "with gsis 0, no birth 0" for all four of 1966, 1967, 1968 and 1969, so the identity rests entirely on the normalised name plus birth date; 17 name-only candidates were rejected by the strict join.

### The backfill creates a new cell-level contradiction the contract does not name

Pre-floor titles are credited while teams, seasons and first_season are deliberately left untouched (correctly, to protect the College Grid). The consequence is that a career answers YES on Won a Super Bowl for a title won with a franchise the key says he never played for. Herb Adderley is the clean case: teams is exactly ['DAL'], first_season 1970, and the backfill hands him SB I and II, both won with Green Bay, taking him to 3. On a board he will answer yes to Won a Super Bowl and no to Played for Packers. That is the section 7 truncated-franchise follow-up, but this round manufactures 89 fresh instances of it, so it belongs in the risks section rather than only in the follow-ups.

Evidence: Live row: Herb Adderley teams ["DAL"], first_season 1970, last_season 1972, sb_wins 1. sbBackfill.mjs: "Herb Adderley today 1 would gain 2 -> 3". Same shape for Bart Starr (gains 2, teams ['GB'] so benign) and Sam Walton (gains 1, keyed to TEN, won with the Jets).

### name_norm is mis-described, and it is dead weight to the page
File: C:\Users\antho\ballpark-hero\.claude\worktrees\r611-college-grid\src\lib\nflGrid.ts

The risk section says the shipped values "carry a trailing space", implying the dup set. Measured: all 22,008 rows equal the UNTRIMMED normalisation of display_name, 1,641 carry a trailing space and 1 carries a leading space (a display name beginning with an apostrophe). dup is 1,584, so the deviating set is not the dup set and the count is 1,642. Also, the page never reads the column: the select list is display_name, teams, pos, draft_round, draft_pick, undrafted, pass4k, rush1k, rec1k, sb_wins, and the client re-normalises display_name itself. The only live reader I found anywhere is a docs SQL file. So step 3b.6 ("grep for readers of name_norm before changing it") has one answer and it is not a blocker, and the fence in 4a section 6 is the right call regardless.

Evidence: SQL: eq_display_untrimmed 22008, eq_display_trimmed 20366, trailing_space (name_norm <> btrim) 1642, name_norm like '% ' 1641, neither 0; dup 1584. src/lib/nflGrid.ts line 175 select list. Grep for name_norm across src, scripts and supabase hits only the migrations, the College Grid code and docs/design/round-613-grid-cache-purge-candidates.sql line 99.

### The 1982 and 1990 boundary note is right but names the wrong mechanism

Section 2a asks the builder to write that 1990 ending at 25 and 1982 at 27 are "consistent with forfeited first round selections", alongside 2008, 2016 and 2023 deriving 31. Two different mechanisms are being lumped together. I two-source verified both: 1982 had 27 first-round picks because New Orleans forfeited theirs for taking Dave Wilson in the 1981 supplemental draft, and 1990 had 25 because Dallas, Denver and Phoenix all forfeited for 1989 supplemental selections. 2008, 2016 and 2023 are single penalty forfeits. Write the supplemental-draft cause for 1982 and 1990 explicitly, since a reviewer who knows there were 28 teams in both years will otherwise flag the boundary as corrupt, which is exactly what the note is meant to prevent.

Evidence: Wikipedia 1982 NFL draft: 27 first-round picks, last is Lester Williams to New England at 27, first of round 2 is Leo Wisniewski to Baltimore at 28, New Orleans forfeited. Wikipedia 1990 NFL draft: Dallas, Phoenix and Denver forfeited, first pick of round 2 is Alexander Wright at 26. Both match the table exactly, including its own forfeit placeholder row at 1990 pick 24 for Denver. Pro Football Reference returns 403 to WebFetch, so Wikipedia plus the table's own forfeit rows are the two sources.

### Small corrections to section 1a and the cleaning claim

Three minor things. (1) Section 3b says the cleaning "does not move any boundary between 1966 and 1984": it moves no boundary in ANY year, 1936 to 2025, so the hedge is unnecessary and the stronger statement is true. (2) Section 1a calls out 1968 and 1969 as uninformative but 1966 (305 rows, all round 1, max pick 305) and 1967 (445 rows, all round 1, max pick 445) are identical in shape. They cost nothing today only because the key holds no career drafted before 1968; if roster coverage ever reaches back past 1970 the 292 grows. Worth one line in the module header. (3) The cleaned count 26,939 is 28,015 minus 75 forfeits, minus 1 row with a null year or non-positive pick, minus 1,000 duplicate (year, pick) rows. The contract only mentions the forfeits and the repeated imports.

Evidence: SQL comparing boundaries from raw rows against boundaries from cleaned rows returns the empty set across all 90 years. Per-year 1966 to 1971 shape: 1966 rows 305 r1 305 maxr 1, 1967 445/445/1, 1968 462/462/1, 1969 442/442/1, 1970 442 r1 156 maxr 12. raw_total 28015, forfeit_rows 75, bad_rows 1, cleaned 26939. careers_by_predraft_year is exactly "1968:138 1969:154".

### What reproduced exactly, so the parent knows the rest of the contract is measured

Everything not listed above reproduced to the digit. Draft table: 28,015 raw, 75 forfeits, 26,939 cleaned, 90 years 1936 to 2025, 23 boundary-less years and the exact list, 623 blocks over 70 years, 2 overlapping, 4 non-contiguous (3 since 1970), the 1994 strays at picks 223 and 224, 1946 round 6 and 1971 round 10. Key: 22,008 rows, draft_round=1 2,193, >=6 3,917, pick with no round 204, undrafted 4,534, no draft record 4,756, careers with a title 2,383, empty pos 0 (so the position unknown branch of 2c is dead code today, do not write it). Derived rule: 275 filed round 1 past their boundary with the per-year split 1970:26 1971:38 1972:45 1973:43 1974:27 1975:45 1976:9 1977:19 1982:23, 292 in boundary-less years (1968:138, 1969:154), all 20 denied first rounders by name with NULL rounds, 1,646 derived first rounders, late 4,272 derived with 355 denied and 0 falsely accused, 292 with no round 6 row. Unknown accounting closes exactly: 4,756 + 292 = 5,048, and pick-without-year, year-without-pick, undrafted-with-pick and round-without-pick are all 0. Franchise floors all match including the derived column (HOU 56/56, CAR 74/75, BAL 84/86, JAX 86/88; late BAL 105/106, HOU 119/123, CAR 128/134; undrafted PIT 167, BAL 213, MIN 213; SB CIN 70, HOU 74, JAX 79) and the heavy losses (GB 149 to 101, BUF 142 to 101, MIN 122 to 97, PIT 117 to 99). Committed file: 4.92 MB, generatedOn 2026-09-02, round 404, 22,008 players, agrees with the table on every count I checked. The four pre-1970 CSV byte sizes are exact to the byte (188,277 / 201,935 / 211,557 / 208,047). super_bowls 60 rows 1967 to 2026 with SB I to V as stated. All twelve named player pins verified against the boundaries (Manning 1998 pick 1 with boundary 30, Brady 2000 pick 199 with boundary 31 and round 6 starting at 167, Montana 1979 pick 82 with boundary 28 and round 6 at 138, Marino 1983 pick 27 with boundary 28, Mahomes 2017 pick 10, Henry 2016 pick 45, Warner undrafted, Csonka 1968 pick 8 unknown). First-season cohorts 1,213 / 1,056 / 1,061 / 1,296 / 640 one-franchise. game_completions football-grid 19 rows, first 2026-07-03 20:52:35Z, last 2026-09-11 02:07:24Z, 2 in 30 days, and every sibling grid count. ai_validation_cache football-grid 204 rows, last written 2026-09-02. RLS on nfl_grid_players is the single "Allow public read" SELECT policy. Source claims all land on the stated lines: nflGrid.ts 153 to 160, useFootballGrid.ts 194 and 211, simNflGrid.mjs line 116 printing "clears 40 qualifiers" against line 126 failing at 30, and line 141 building the First Round expectation from p.draft.round === 1 (the tautology), simNflGridData.mjs line 108 okDraft accepting any finite round, line 261 selecting neither name nor name_norm, thirteen recorded careers at lines 174 to 188, the table control at line 267 absent from the docstring, and simNflGridPage.mjs line 60 requiring playerMatchesCell by name.

Evidence: Roughly twenty read-only SELECTs against flawuiqbvjobmkfkauhw plus node reads of the two committed keys and the cached rosters. Scratch work in C:\Users\antho\AppData\Local\Temp\claude\C--Users-antho-ballpark-hero\44bf65a6-5087-4325-b3e8-5708903af4e6\scratchpad\r614 (sbBackfill.mjs and roster_1966 to 1969 csv). No repo file was edited, no harness or build was run.

- Open question: Where did 515 come from? It matches no threshold on the draft_round=1 population (24 to 40 scanned). If it came from a different population, say all careers rather than round-1 careers, the sentence should say so; otherwise drop it and use 503 = pick > 36, which does reproduce.
- Open question: Does the builder want the last-round exactness fix (treat start(rMax) <= pick <= that year's max pick as exact)? It recovers the exact round for 1,825 careers, 1,546 of them already correct today, and costs nothing that I can see. If the lower bound is kept deliberately, the round should measure and record the 1,825 as a known loss.
- Open question: Should the 520 recoverable draft records from the College Grid key be folded into this round or booked as a named follow-up with the number? Twelve of them are first rounders the NFL grid will answer UNKNOWN on, including Jim Kelly and Bo Jackson, while the sibling game answers YES from the same table.
- Open question: Section 4a section 9 asks the harness to hold the newly credited count "at the first run's value (89 on the investigation's join, re-measure)". My independent re-measure agrees at 89 but the bridge I built comes only from the cached 1970 to 2001 files. The generator builds its bridge over the modern roster rows first. No career here reaches 2002 so it cannot differ, but the builder should confirm on the real run rather than inherit my shortcut.
