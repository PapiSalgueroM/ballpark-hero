# Agent A: the desktop lane (Claude Code on Anthony PC)

OWNS: platform and data. Supabase (schema, validation, provenance, edge
functions, migrations), the ticker and its feeds, live-site verification and
publishing (Lovable deploy with the latest_commit_sha check), long browser
harness runs, data pulls needing web verification, the architecture and SEO
documents, and the repo skills.

High-risk shared files it treats as coordination zones: App.tsx routing,
gameRegistry.ts, global layout components, package.json, tailwind config,
supabase migrations.

Its CURRENT work, claims and status live ONLY on docs/WORKBOARD.md. This file
is the stable ownership contract, not a status page; a status page that
duplicates the board rots.
